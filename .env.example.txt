# ==============================================================================
# SISTEMA DE ENCAMINHAMENTO DE EMAILS - CP FANI
# ==============================================================================
# INSTRUÇÕES:
# 1. Copie este arquivo para `.env` (No Windows: copy .env.example .env)
# 2. Preencha as variáveis abaixo com suas credenciais reais.
# 3. NUNCA commite o arquivo `.env` no Git (ele já está no .gitignore).
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. CONFIGURAÇÕES IMAP (Leitura de Emails - Conta de Origem)
# ------------------------------------------------------------------------------
# Para Gmail, é OBRIGATÓRIO usar uma "Senha de App" (App Password).
# Não use sua senha normal de login do Google.
# Link para gerar: https://myaccount.google.com/apppasswords
IMAP_SERVER=imap.gmail.com
IMAP_PORT=993
IMAP_USER=alex@didier.com.br
IMAP_PASSWORD=XXXX-XXXX-XXXX-XXXX

# ------------------------------------------------------------------------------
# 2. CONFIGURAÇÕES SMTP (Envio de Emails - Encaminhamento)
# ------------------------------------------------------------------------------
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=alex@didier.com.br
SMTP_PASSWORD=XXXX-XXXX-XXXX-XXXX
SENDER_EMAIL=alex@didier.com.br
SENDER_NAME=Alex Didier (Encaminhamento)

# ------------------------------------------------------------------------------
# 3. DESTINO E AÇÕES
# ------------------------------------------------------------------------------
FINANCEIRO_EMAIL=financeiro@didier.com.br

# Emails internos do sistema para processar cliques nos botões
# (O script vai monitorar a caixa de entrada buscando respostas para estes endereços)
APPROVE_EMAIL=approve@cp-fani.local
REJECT_EMAIL=reject@cp-fani.local

# ------------------------------------------------------------------------------
# 4. REGRAS DE NEGÓCIO E CAMINHOS
# ------------------------------------------------------------------------------
# Data inicial para coleta de emails (Formato YYYY-MM-DD)
START_DATE=2026-08-01

# Caminhos relativos (O config/settings.py usará pathlib para resolver absolutos)
DATA_DIR=./data
LOG_DIR=./logs
BLACKLIST_FILE=./data/blacklist.txt
DB_FILE=./data/pending_emails.db